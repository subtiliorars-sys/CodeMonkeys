# Pinned to exact versions for reproducible builds (Fly Docker, CI).
# Bump deliberately: change a pin here, re-run `python -c "import server"` + tests, PR it.
# Full --require-hashes locking is a follow-up: it must be generated on the
# Linux/py3.12 target (pydantic-core, cryptography ship per-platform wheels), so
# it can't be produced reliably from a Windows dev host without breaking the build.
fastapi==0.136.3
# Security: floor Starlette at the patched release for CVE-2026-48710 (auth bypass).
# FastAPI pulls Starlette transitively; pin a floor so a resolved version below the
# fix can never slip in. Bump the floor (not a hard ==) so FastAPI's own range still resolves.
starlette>=1.0.1
uvicorn==0.49.0
pydantic==2.13.4
requests==2.34.2
pyotp==2.9.0
anthropic==0.106.0
fido2==2.2.0
segno==1.6.6
cryptography==48.0.1
google-auth==2.48.0
# Terminal CLI (cli/) rendering only — no server-side usage.
rich==14.2.0
